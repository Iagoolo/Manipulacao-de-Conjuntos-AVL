#ifndef CLEAR_HPP
#define CLEAR_HPP

// Função que irá limpar o terminal automaticamente
void clearTerminal();

// Função que irá limpar o terminal após o usuário pressionar enter
void clearTerminalWithWait(bool waitForEnter = false);

#endif